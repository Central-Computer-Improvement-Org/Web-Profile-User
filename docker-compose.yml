version: '3'

services:
  nextjs:
    container_name: cci-web-profile
    build:
      context: ./
    image: fe-cci-web-profile
    ports:
      - '3000:3000'
    restart: unless-stopped
    networks:
      - nextjs-network

networks:
  nextjs-network:
    driver: bridge
